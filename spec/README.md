CoffeeScript 1.3.3 needed.

Generate specs and core javascript files by running

$ cake build_tests
$ rake jasmine
$ open http://localhost:8888

Use Guard for automatic compiling.

$ bundle install
$ bundle exec guard
